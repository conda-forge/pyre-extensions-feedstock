{% set name = "pyre-extensions" %}
{% set version = "0.0.29" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyre-extensions-{{ version }}.tar.gz
  sha256: 4113600706c47306a156dcc9c405f4d458c505be432699571e357ef20e2d5d77

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - typing_inspect
    - typing-extensions

test:
  imports:
    - pyre_extensions
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pyre-check.org
  summary: Type system extensions for use with the pyre type checker
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
